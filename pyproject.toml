[project]
name = "prueba-aux"
version = "0.1.0"
description = ""
authors = [
    {name = "SebastianBetancur08",email = "s.betancur@ude.edu.co"}
]
requires-python = ">=3.11,<4.0"
dependencies = [
    "fastapi (==0.135.3)",
    "uvicorn[standard] (==0.44.0)",
    "persistence-kit[dynamodb] (==3.4.0)",
    "pydantic (>=2.11,<3)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (==9.0.3)",
    "pytest-mock (>=3.15.1,<4.0.0)",
    "httpx (==0.28.1)",
    "sqlmodel (==0.0.38)"
]

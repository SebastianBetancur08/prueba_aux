# ─── Core API ───────────────────────────────────────────────────
fastapi==0.135.3          # última estable (Apr 1, 2026)
uvicorn[standard]==0.44.0 # última estable (Apr 6, 2026)

# ─── HTTP / Testing ─────────────────────────────────────────────
httpx==0.28.1             # última estable; usada internamente por FastAPI TestClient
requests==2.33.0          # última estable (Mar 30, 2026)
httpie                    # sin pin necesario, solo CLI

# ─── Ciencia de datos ───────────────────────────────────────────
numpy==2.4.4              # última estable (Mar 29, 2026)

# ─── Utilidades esenciales ──────────────────────────────────────
python-dotenv==1.1.0      # estable, sin cambios breaking
python-multipart==0.0.20  # última estable (Apr 10, 2026); requerida por FastAPI
email-validator==2.2.0    # estable con Pydantic v2

# ─── Seguridad ──────────────────────────────────────────────────
python-jose[cryptography]==3.3.0  # última estable (May 2025); sin actualizaciones recientes
passlib[bcrypt]==1.7.4            # última estable; proyecto en mantenimiento pasivo
bcrypt==4.2.1           

passlib[bcrypt]==1.7.4
bcrypt==4.2.1 

# ─── Base de datos y validación ─────────────────────────────────
sqlmodel==0.0.38          # ← ancla principal (Apr 2, 2026)
  # instala automáticamente:
  #   pydantic>=2.x  →  2.11.x (Apr 13, 2026)
  #   sqlalchemy>=2.0.14  →  2.0.49 (Apr 3, 2026)

# ─── Desarrollo ─────────────────────────────────────────────────
pytest==9.0.3             # última estable (Apr 7, 2026)
pytest-mock               # sin pin; compatible con pytest 9.x

# Python 3.12.3 para el entorno usando python3.12 al crear el entorno